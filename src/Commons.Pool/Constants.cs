using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Commons.Pool
{
    internal static class Constants
    {
        public const string InvalidInitialSize = "The pool initial size is invalid";

        public const string InvalidMaxSize = "The pool max size is invalid";
    }
}
